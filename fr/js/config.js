/**
 * DCM CONFIGURATION — v1.0
 * Centralized configuration for different environments.
 */

window.DCM_CONFIG = {
    VERSION: '4.3.2',
    ENV: 'production',
    API_URL: 'https://api.dcmcore.com',
    FEATURES: {
        USE_SUPABASE: true,
        ENHANCED_AUDIT: true,
        AI_COPILOT: true
    }
};
